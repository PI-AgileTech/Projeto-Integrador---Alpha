@model IEnumerable<Alpha.Models.checkin>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.hospede1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.horario_checkin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.horario_checkout)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.num_quarto1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.hospede1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.horario_checkin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.horario_checkout)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.num_quarto1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.checkin_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.checkin_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.checkin_Id })
        </td>
    </tr>
}

</table>
